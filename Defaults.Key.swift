import Foundation

extension Defaults {
    enum Key: String {
        case
        id,
        beads,
        match,
        game,
        victories,
        onboard_start,
        onboard_beads,
        settings_sound,
        settings_vibrate
    }
}
