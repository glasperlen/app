import Foundation

extension Defaults {
    enum Key: String {
        case
        beads,
        match,
        victories,
        onboard_start,
        settings_sound,
        settings_vibrate
    }
}
