import SwiftUI

extension Game {
    struct PrizeRobot: View {
        @Binding var session: Session
        
        var body: some View {
            Finish {
                
            }
        }
    }
}
